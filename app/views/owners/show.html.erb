<div class="page-header">
  <%= link_to owners_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to edit_owner_path(@owner), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
    Edit Owner
  <% end %>
  <h1 class="pg-title">Logged in as <%= @owner.email %></h1>
</div>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Cuisine</th>
      <th>Menu</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @owner.trucks.each do |truck| %>
      <tr>
        <td><%= truck.name %></td>
        <td><%= truck.cuisine %></td>
        <td><%= truck.menu %></td>
        <td><%= link_to 'View Truck', truck, class: "btn btn-primary" %></td>
        <% if truck.owner == current_owner %>
          <td><%= link_to 'Edit Truck Info', edit_truck_path(truck), class: " btn btn-default" %></td>
          <td><%= link_to 'Remove Truck', truck, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary" %></td>
          <td><%= link_to 'Create a New Location', new_time_and_place_path() + "?truck_id=" + truck.id.to_s, class: "btn btn-default" %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<h1 class="pg-title">Add a New Truck</h1>
<%= render '/trucks/form' %>
